function unique (array, index, value){
    return array.indexOf(value) === index;
}


function sumArr (array) {
    let sum = 0;
    for(let i=0; i++; i<= array.length) {
        sum += array[i]
    }
    return sum
}